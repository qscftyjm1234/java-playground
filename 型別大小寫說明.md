# 💡 小寫 vs 大寫型別 - 簡單理解

## 🎯 核心概念

### 小寫 = 純粹的值 (基本型別)

- **就是一個值**,背後沒有任何可以用的方法
- 直接存放資料
- 效能快、省記憶體

```java
int age = 25;
// age 就只是數字 25
// 沒有方法可以呼叫
```

### 大寫 = 被包裝過的物件 (包裝類別)

- **是一個物件**,被包裝過的
- 可以把東西丟進去
- 有對應的方法可以用

```java
Integer age = 25;
// age 是一個物件,裡面裝著 25
// 有很多方法可以用
age.toString()     // 轉字串
age.compareTo(20)  // 比較大小
```

---

## 📊 對照表

| 特性       | 小寫 (基本型別) | 大寫 (包裝類別) |
| ---------- | --------------- | --------------- |
| **本質**   | 純粹的值        | 包裝過的物件    |
| **方法**   | ❌ 沒有         | ✅ 有很多       |
| **速度**   | ⚡ 快           | 🐢 較慢         |
| **記憶體** | 💾 小           | 📦 較大         |
| **null**   | ❌ 不能是 null  | ✅ 可以是 null  |

---

## 🎁 包裝的比喻

### 小寫 = 裸露的糖果 🍬

```java
int num = 100;
// 就是 100,沒有包裝
// 沒有額外功能
```

### 大寫 = 包裝好的糖果盒 📦

```java
Integer num = 100;
// 100 被放在盒子裡
// 盒子有標籤、說明、功能
```

---

## 📝 完整對應

| 小寫 (值) | 大寫 (物件) |
| --------- | ----------- |
| `int`     | `Integer`   |
| `double`  | `Double`    |
| `boolean` | `Boolean`   |
| `char`    | `Character` |
| `long`    | `Long`      |
| `float`   | `Float`     |
| `byte`    | `Byte`      |
| `short`   | `Short`     |

---

## 💡 記憶口訣

```
小寫 = 值 = 沒方法
大寫 = 物件 = 有方法
```

**例外:** `String` 一開始就是大寫,因為它本來就是類別!

---

## ✅ 你的理解完全正確!

> 小寫就是一個值,背後沒有任何可以用的方法
> 大寫是有被包裝過的,可以把東西丟進去,有對應的方法

**這個理解 100% 正確! 👍**

---

## ⚠️ 重要:String 沒有小寫版本!

### ❌ 常見誤解

很多新手會問:

> "我只是要存一個值,不需要用方法,可以用小寫的 `string` 嗎?"

**答案:不行! Java 沒有小寫的 `string`!**

### 📋 為什麼?

```java
// ❌ 錯誤!這個不存在
string name = "小狗";  // 編譯失敗!

// ✅ 正確!只有這個
String name = "小狗";  // 必須大寫 S
```

**原因:**

- `String` 從一開始就是**類別**,不是基本型別
- Java 沒有設計小寫的 `string`
- 即使你不需要用方法,也必須用 `String`

### 🔄 和 int 的差異

| 型別 | 小寫版本              | 大寫版本                |
| ---- | --------------------- | ----------------------- |
| 整數 | `int` ✅ (基本型別)   | `Integer` ✅ (包裝類別) |
| 文字 | `string` ❌ (不存在!) | `String` ✅ (類別)      |

**重點:**

- `int` 有兩種選擇:小寫 `int` 或大寫 `Integer`
- `String` 只有一種選擇:大寫 `String`

### 💡 實際使用

```java
// 即使不用方法,也必須用 String
String name = "小狗";
System.out.println(name);  // 只存值、印出,不用方法也 OK

// 當然也可以用方法
String text = "hello";
text.length();          // 可以用
text.toUpperCase();     // 可以用
```

### ✅ 記住

1. **Java 只有 `String` (大寫),沒有 `string` (小寫)**
2. **即使只存值不用方法,也必須用 `String`**
3. **這是 Java 的設計,無法改變**

---

## 🎓 新人常見錯誤

### 錯誤 1: 用小寫 string

```java
string name = "test";  // ❌ 編譯錯誤!
```

**錯誤訊息:**

```
error: cannot find symbol
  symbol:   class string
```

### 錯誤 2: 全大寫 STRING

```java
STRING name = "test";  // ❌ 編譯錯誤!
```

### ✅ 正確寫法

```java
String name = "test";  // ✅ 只有這個正確!
```

---

## 📚 總結

**核心概念:**

- 小寫型別 = 基本型別 = 純值 = 沒方法
- 大寫型別 = 類別/物件 = 包裝過 = 有方法

**String 特殊規則:**

- `String` 只有大寫版本
- 沒有小寫的 `string`
- 這是 Java 的設計,必須遵守
