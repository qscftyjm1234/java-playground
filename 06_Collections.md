# 🎓 第六課：陣列 (Array) 與 ArrayList

對於習慣 JavaScript 的開發者來說，這是 Java 最「囉嗦」的地方之一。在 JS 中一個 `[]` 搞定所有事，但在 Java 中我們會根據「長度是否固定」來選擇工具。

---

## 📦 1. 原生陣列 (Array)

這是 Java 最基礎的容器。

### 語法對照

- **Java**: `String[] arr = {"A", "B"};` (使用大括號 `{}`)
- **JS**: `let arr = ["A", "B"];` (使用中括號 `[]`)

### 特點

1.  **長度固定**：你一旦在 `{}` 裡寫了 3 個東西，這輩子這個陣列就只能裝 3 個。
2.  **效能極佳**：因為結構簡單，電腦跑得很快。
3.  **語法直覺**：讀取（`arr[0]`）與修改（`arr[0] = "X"`）跟 JS 完全一樣。

---

## 🚀 2. ArrayList (推薦使用)

如果你想要像 JS 陣列一樣可以隨時 `push`、隨時 `pop`，那你應該使用 `ArrayList`。

### 常用操作

| 功能     | Java (ArrayList)     | JavaScript (Array)  |
| :------- | :------------------- | :------------------ |
| **新增** | `.add(value)`        | `.push(value)`      |
| **取得** | `.get(index)`        | `[index]`           |
| **長度** | `.size()`            | `.length`           |
| **刪除** | `.remove(index)`     | `.splice(index, 1)` |
| **修改** | `.set(index, value)` | `[index] = value`   |

### 為什麼有 `<String>`？ (泛型)

在 Java 中，容器需要知道它**裝的是什麼型別**。
`ArrayList<String>` 代表這個箱子「只能裝字串」。如果你放個整數進去，編譯器會報錯。這讓你的程式在執行前就確定絕對不會出現「型別混亂」的問題。

---

## 📊 該選哪一個？

- **用 Array `{}`**: 如果你已經確定資料數量（例如：一週有七天、一年有十二個月）。
- **用 ArrayList**: 大部分的情況（例如：使用者清單、購物車、甚至你不確定資料會變多還是變少）。

---

## 🔄 實踐建議

雖然 Array 用 `{}` 看起來讓你很有親切感，但在實務開發中，**`ArrayList` 的出現頻率比 Array 高出非常多**，因為它的功能最接近現代開發的需求。

---

## 🎯 執行練習

```bash
javac Collections.java
java Collections
```

嘗試修改 `Collections.java`：

1. 試著在 `friends` 裡面 `add` 更多人。
2. 試著用 `friends.set(0, "新的名字")` 來修改資料。
