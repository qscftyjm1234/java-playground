# 🎓 第二課: 變數和資料型別

## ✅ 程式執行成功!

你的第二個 Java 程式已經成功執行了! 🎉

**程式輸出:**

```
我的年齡: 25
現在是: 2026 年
價格: $99.99
圓周率: 3.14159
名字: 小明
訊息: Hello, Java!
是學生嗎? true
在下雨嗎? false
成績等級: A
符號: ★

--- 數學運算 ---
加法: 10 + 3 = 13
減法: 10 - 3 = 7
乘法: 10 × 3 = 30
除法: 10 ÷ 3 = 3
餘數: 10 % 3 = 1

原本的分數: 80
新的分數: 95

稅率: 0.05
```

---

## 📚 什麼是變數?

**變數** 就像一個**盒子**,可以用來存放資料。

```
變數名稱 = 盒子上的標籤
資料型別 = 盒子的種類 (放什麼東西)
值 = 盒子裡面的東西
```

### 變數宣告格式

```java
資料型別 變數名稱 = 值;
int     age    = 25;
```

---

## 🎯 Java 的基本資料型別

### 1️⃣ 整數 (int)

```java
int age = 25;        // 年齡
int year = 2026;     // 年份
int score = -10;     // 可以是負數
```

- 用來存放**整數** (沒有小數點)
- 範圍: -2,147,483,648 到 2,147,483,647

### 2️⃣ 小數 (double)

```java
double price = 99.99;    // 價格
double pi = 3.14159;     // 圓周率
```

- 用來存放**小數**
- 比 `float` 更精確 (推薦使用 `double`)

### 3️⃣ 文字 (String)

```java
String name = "小明";
String message = "Hello, Java!";
```

- 用來存放**文字**
- 用**雙引號** `" "` 包起來
- 注意: `String` 的 `S` 要**大寫**!

### 4️⃣ 布林值 (boolean)

```java
boolean isStudent = true;
boolean isRaining = false;
```

- 只有兩個值: `true` (真) 或 `false` (假)
- 用來表示**是或否**的狀態

### 5️⃣ 字元 (char)

```java
char grade = 'A';
char symbol = '★';
```

- 只能存放**一個字元**
- 用**單引號** `' '` 包起來 (不是雙引號!)

---

## 📊 資料型別對照表

| 型別 | 關鍵字    | 範例                     | 說明              |
| ---- | --------- | ------------------------ | ----------------- |
| 整數 | `int`     | `int age = 25;`          | 整數,沒有小數點   |
| 小數 | `double`  | `double price = 99.99;`  | 浮點數,有小數點   |
| 文字 | `String`  | `String name = "小明";`  | 字串,用雙引號     |
| 布林 | `boolean` | `boolean isTrue = true;` | 只有 true/false   |
| 字元 | `char`    | `char grade = 'A';`      | 單一字元,用單引號 |

---

## 🔢 基本運算

### 數學運算子

| 運算子 | 說明 | 範例     | 結果           |
| ------ | ---- | -------- | -------------- |
| `+`    | 加法 | `10 + 3` | `13`           |
| `-`    | 減法 | `10 - 3` | `7`            |
| `*`    | 乘法 | `10 * 3` | `30`           |
| `/`    | 除法 | `10 / 3` | `3` (整數除法) |
| `%`    | 餘數 | `10 % 3` | `1`            |

> [!NOTE] > **整數除法:** 當兩個整數相除時,結果會是整數 (小數部分會被捨棄)。例如 `10 / 3 = 3`,不是 `3.333...`

### 字串連接

```java
String firstName = "小";
String lastName = "明";
String fullName = firstName + lastName;  // "小明"
```

---

## 🔄 變數可以改變

```java
int score = 80;          // 原本是 80
System.out.println(score);  // 輸出: 80

score = 95;              // 改成 95
System.out.println(score);  // 輸出: 95
```

---

## 🔒 常數 (不能改變)

使用 `final` 關鍵字來宣告常數:

```java
final double TAX_RATE = 0.05;
// TAX_RATE = 0.1;  // ❌ 錯誤!常數不能改變
```

**常數命名慣例:** 全部大寫,用底線分隔 (例如: `TAX_RATE`, `MAX_VALUE`)

---

## 💡 重要觀念

### ✅ 好的變數命名

```java
int age = 25;              // ✅ 清楚明瞭
String userName = "John";  // ✅ 駝峰式命名
double totalPrice = 99.99; // ✅ 有意義
```

### ❌ 不好的變數命名

```java
int a = 25;        // ❌ 太簡短,不知道是什麼
String s = "John"; // ❌ 沒有意義
double x = 99.99;  // ❌ 看不出用途
```

### 命名規則

- ✅ 可以用字母、數字、底線 `_`、錢字號 `$`
- ❌ 不能用數字開頭
- ❌ 不能用 Java 關鍵字 (例如: `int`, `class`, `public`)
- ✅ 建議用駝峰式命名 (camelCase): `userName`, `totalPrice`

---

## 🎯 練習建議

1. **修改變數值** - 改變 `age`, `name` 等變數的值
2. **加入新變數** - 宣告自己的變數 (例如: 身高、體重)
3. **做數學運算** - 試試不同的數字和運算子
4. **測試常數** - 試著改變 `final` 變數,看看會發生什麼

---

## 🚀 下一課預告

**第三課: 使用者輸入**

- 如何讓使用者輸入資料
- 使用 `Scanner` 類別
- 互動式程式

準備好繼續了嗎? 😊
